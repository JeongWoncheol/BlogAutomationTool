# 왈라랜드 기반 착장 콘텐츠

착장 기능의 기본 데이터 소스는 `wala-land.com`이다. 기존 Ollama 텍스트 모델과 Qwen3-VL 모델 설정은 변경하지 않았다.

## 바로 사용하기

1. `START_STUDIO.cmd`를 실행한다.
2. 왼쪽 **연예인 착장 트렌드**를 연다. 페이지 제목은 **왈라랜드 착장 콘텐츠**다.
3. 기간은 **전체**, 회차 수집량은 **50**으로 시작한다. 회차 수집량은 1~500개다.
4. **다음 회차 수집**을 누르면 전체 공개 목록을 확인하고 아직 저장하지 않은 원문부터 수집한다. 다시 누르면 이어서 진행한다.
5. **수집 → 원고 자동 실행**은 해당 검색에 일치하는 저장 콘텐츠를 분석하고, 소개 상품을 연결한 뒤 원고를 만든다. 이미 완성한 원고는 유지한다.
6. 검토할 행을 선택하면 원문·상품·원출처 링크와 원고를 상세 영역에서 볼 수 있다. **작성물로 보내기**는 연결 가능한 상품이 있는 원고를 기존 작성물 관리로 옮긴다.

**작업 중지**는 현재 요청 또는 AI 처리가 끝나면 중지한다. 이미 저장한 데이터는 남는다. 원문 접근이 제한되면 해당 회차를 중지하고 사유를 표시한다.

검색은 제목·영문 제목·태그를 대상으로 한다. 인물 또는 기간 조건은 **현재까지 저장한 원문**에 적용한다. 첫 50개에서 이름을 못 찾았다고 전체 사이트에 없는 것은 아니다. 남은 원문을 수집한 뒤 다시 검색한다. 전체 목록에는 공개 패션·뷰티 콘텐츠가 함께 있을 수 있다.

## 필요한 설정

- 왈라랜드 원문·이미지 수집에는 별도 API 키나 Chrome 확장이 필요 없다. 인터넷 연결이 필요하다.
- Python 의존성에 `httpx2[http2,brotli,zstd]`, `pydantic`을 추가했다. 이 PC에는 필요한 패키지를 설치했다. 다른 PC에서는 기존 설치 스크립트 또는 `python -m pip install -r requirements.txt`로 설치한다.
- AI 문장 생성은 기존 Ollama 연결을 그대로 사용한다. 준비되지 않았으면 확인된 상품과 출처로 기본 원고를 만든다. Vision이 없으면 사진을 통한 보조 분석은 생략한다.
- 네이버 텍스트 임시저장은 **계정·연결**의 대상 블로그 주소 설정과 전용 Chrome의 네이버 로그인이 필요하다. 착장 원고는 사실에 맞는 관련 태그를 그대로 사용하며 일반 상품의 최소 20개 기준을 적용하지 않는다. 공개 발행은 수행하지 않는다. [개선사항 사용 안내](IMPROVEMENTS_KR.md)를 참고한다.

## 저장되는 정보와 범위

공개 콘텐츠 사이트맵의 모든 ID를 기록하며, 원문은 회차별로 수집한다. 원문 ID로 중복을 방지한다. 삭제·비공개 상태의 페이지는 콘텐츠로 사용하지 않고, 일시적 수집 오류는 다음 회차에서 재시도한다.

원문마다 제목, 게시일, 태그, 설명, 사진 URL, 원출처 표기, 직접 연결 상품의 브랜드·이름·표시 가격·링크·이미지 URL을 저장한다. 추천 상품 목록은 직접 연결 상품과 구분한다. 페이지에서 제공하는 전체 `CONTENT_DETAIL` 원본 JSON도 보존한다. 계정·주문·비공개 데이터나 원출처의 다른 사이트를 수집하지 않는다. 상품의 별도 상세 페이지 전체를 추가 크롤링하는 기능은 포함하지 않는다.

| 위치 | 내용 |
|---|---|
| `data/studio.db` → `wala_archive` | 전체 공개 목록, 수집 상태, 구조화한 원문과 원본 JSON |
| `data/studio.db` → `celebrity_style_candidates/items` | 착장 후보·소개 상품·연결 상태·원고 |
| `outputs/celebrity_style_candidates.csv` | 모든 저장 후보의 요약 |
| `outputs/celebrity_style_items.csv` | 소개 상품과 연결 URL |
| `outputs/wala_archive.jsonl` | 저장한 원문 전체 내보내기. 한 줄에 원문 하나, 원본 JSON 포함 |
| `evidence/celebrity_style/<후보ID>/references` | 선택적으로 내려받은 근거 사진 |

**CSV 열기**를 누르면 위 CSV 두 개와 JSONL을 갱신하고 후보 CSV를 연다. 목록은 화면에서 300개씩 이동할 수 있다. 원문 캐시는 수집 당시 스냅샷이며, 이미 수집한 글의 수정 내용이나 최신 판매 가격을 자동 갱신하지는 않는다.

## 원고에서 지키는 구분

- 게시일은 실제 촬영일·착용일이 아니다. 사이트맵의 자동 생성 `lastmod`를 게시일로 사용하지 않는다.
- 직접 연결 상품은 **왈라랜드가 소개한 상품**으로 표시한다. 단일 매체의 정보를 독립적으로 교차 검증한 착용 확정 정보로 바꾸지 않는다.
- 원문에 가격이 없으면 0원 판매가로 표시하지 않는다. 원문 가격은 현재 판매 가격과 다를 수 있다.
- 추천 상품은 실제 착용 상품으로 섞지 않는다. 상품 정보가 없는 글에는 브랜드나 모델을 추측해서 넣지 않는다.
- 원문 사진은 근거 확인용이다. 사진과 원문 문장을 블로그에 자동 복제하지 않는다. 작성물로 옮긴 Wala 원고에는 기존 쇼핑몰 이미지·3사 가격·쿠팡 제휴링크 검색이 적용되지 않는다. 이미지 단계는 기존 검증된 상품 사진만 보존하며, 없으면 확인 대기로 표시한다.

## 이번 실행에서 확인한 상태

2026-09-07 확인: 공개 목록 **5,985개**, 실제 원문 **22개 저장**, 원고 **21개 생성**, 연결 상품 **15개**, 작성물 **1개 연동**. 전체 5,985개 원문 수집을 완료한 상태는 아니다. 이번 확인에서 최신 원문의 게시일은 2025-10-10이었다.

실제 수집·이어받기·원고·상품 연결·작성물 연동, 빈 선택·중지, 삭제 글 제외, 추천 상품 분리, 외부 소스 차단, 일반 상품의 기존 처리 유지, 원본 내보내기를 검증했다. 데스크톱 및 최소 창 크기에서 착장 화면도 확인했다. 현재 PC의 Ollama/Vision 모델이 준비되지 않아 실제 AI 추론과 네이버 로그인 후 임시저장은 이번에 검증하지 않았다.
